const express = require('express');
const router = express.Router();

// Placeholder addresses routes - will be implemented later
router.get('/', (req, res) => {
  res.json({ message: 'Addresses routes coming soon' });
});

module.exports = router; 