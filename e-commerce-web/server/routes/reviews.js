const express = require('express');
const router = express.Router();

// Placeholder reviews routes - will be implemented later
router.get('/', (req, res) => {
  res.json({ message: 'Reviews routes coming soon' });
});

module.exports = router; 