const express = require('express');
const router = express.Router();

// Placeholder orders routes - will be implemented later
router.get('/', (req, res) => {
  res.json({ message: 'Orders routes coming soon' });
});

module.exports = router; 