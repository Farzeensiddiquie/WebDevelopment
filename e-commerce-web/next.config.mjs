/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
   domains: [ 'images.unsplash.com',
      'cdn.shopify.com',
      'plus.unsplash.com',
      'source.unsplash.com',
      'localhost']
  },
};

export default nextConfig;
