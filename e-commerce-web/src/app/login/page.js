export const metadata = {
  title: "Login",
  description: "Welcome to Farzeen Finds — your destination for curated fashion, lifestyle picks, and inspiration by Farzeen Wasif.",
};

import LoginClient from "./LoginClient";

export default function LoginPage() {
  return <LoginClient />;
}
